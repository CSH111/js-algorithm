// 템플릿 예시 - 실제 사용시 이 파일을 복사해서 사용하세요
// Jest는 실제 테스트가 없는 파일을 허용하지 않으므로 이 파일은 테스트로 실행되지 않습니다.

/*
const { runTests, createTestCase, createErrorCase } = require('./test-utils');

function yourFunction(param) {
  // 여기에 알고리즘 구현
}

const testCases = [
  createTestCase('기본 케이스', [입력값], 예상결과),
  createTestCase('빈 배열 테스트', [[]], []),
  createTestCase('단일 요소 테스트', [[1]], [1]),
  createErrorCase('에러 케이스', [잘못된입력], '에러메시지'),
];

runTests('함수명', yourFunction, testCases);
*/